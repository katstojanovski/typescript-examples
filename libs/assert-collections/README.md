# assert-collections

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build assert-collections` to build the library.

## Running unit tests

Run `nx test assert-collections` to execute the unit tests via [Jest](https://jestjs.io).
